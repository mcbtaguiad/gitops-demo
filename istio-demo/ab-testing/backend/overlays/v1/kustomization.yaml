resources:
  - ../../base

nameSuffix: -v1

patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: backend

  - path: service-patch.yaml
    target:
      kind: Service
      name: backend
